<div class="container">
  <h3>Your search results</h3>
  <div class="cards">
    <% @cars.each do |car| %>
      <%= link_to car_path(car) do %>
        <%= render 'shared/card', car: car %>
      <% end %>
    <% end %>
  </div>
</div>
